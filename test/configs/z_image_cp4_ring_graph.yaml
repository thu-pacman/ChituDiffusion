launch:
  tag: zimage-cp4-wallclock
  backend: torchrun
  num_nodes: 1
  gpus_per_node: 4
  python_script: test/test_z_image.py
  srun:
    partition: debug
    cpus_per_gpu: 24
    job_name: chitu-zimage-cp4-wallclock

runtime:
  chitu_debug: true
  cuda_launch_blocking: false

model:
  name: Z-Image
  ckpt_dir: /dockerdata/Z-Image

parallel:
  cfp: 1
  up: 1

infer:
  attn_type: flash_attn
  low_mem_level: 0

eval:
  eval_type: []
  reference_path: null

output:
  root_dir: outputs/test-cp4-wallclock
  run_log: true
  memory: true
  timer: true
  log_ranks: [0]

overrides:
  - models.sampler.sample_steps=10
  - models.sampler.guidance_scale=[1.0]
