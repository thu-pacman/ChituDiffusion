from setuptools import setup


# Project metadata, package discovery, dependencies, and entry points are owned
# by pyproject.toml. This file remains only for tooling that still invokes
# ``python setup.py``.
setup()
