launch:
  tag: qwen-smoke
  num_nodes: 1
  gpus_per_node: 1
  python_script: test/test_qwen_image.py
  srun:
    partition: debug
    cpus_per_gpu: 24
    job_name: chitu-qwen-smoke

runtime:
  chitu_debug: true
  cuda_launch_blocking: false

model:
  name: Qwen-Image
  ckpt_dir: /home/chenyy/WORK/models/Qwen-Image

parallel:
  cfp: 1
  up: 1

infer:
  attn_type: torch_sdpa
  low_mem_level: 0

eval:
  eval_type: []
  reference_path: null

output:
  root_dir: outputs/test-smoke
  run_log: true
  memory: true
  timer: true
  log_ranks: [0]

overrides:
  - models.sampler.sample_steps=4
