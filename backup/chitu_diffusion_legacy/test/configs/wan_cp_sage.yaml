launch:
  tag: wan-cp-sage
  num_nodes: 1
  gpus_per_node: 2
  python_script: test/test_flexcache.py
  srun:
    partition: debug
    cpus_per_gpu: 24
    job_name: chitu-wan-cp-sage

runtime:
  chitu_debug: true
  cuda_launch_blocking: false

model:
  name: Wan2.1-T2V-1.3B
  ckpt_dir: /home/chenyy/WORK/models/Wan2.1-T2V-1.3B

parallel:
  cfp: 1
  up: 2

infer:
  attn_type: sage
  low_mem_level: 0

eval:
  eval_type: []
  reference_path: null

output:
  root_dir: outputs/test-smoke
  run_log: false
  memory: false
  timer: true
  log_ranks: [0]

overrides: []
